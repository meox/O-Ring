RustRing with async-std
--------

Compile this test using cargo:

`cargo build --release`

And run with:

`cargo run --release  -- 200000 30`

or alternatively, with:

`./target/release/rustring 200000 30`